{
  "name": "stream-prompt-manager",
  "private": true,
  "version": "0.0.0",
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "turbo run preview",
    "generate-og": "turbo run generate-og",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
