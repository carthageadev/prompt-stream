# Stream Prompts API

FastAPI backend for Stream Prompts application.

## Setup

```bash
uv sync
```

## Run

```bash
uv run uvicorn app.main:app --reload --port 8000
```

## Deploy to Vercel

```bash
vercel deploy
```
